spring.datasource.url=jdbc:mysql://localhost:3306/taskask?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=first181104@Mysql

# Hibernate auto-update: will create/alter tables to match entities.
# Since you already created tables, this will try to adjust them if they differ.
spring.jpa.hibernate.ddl-auto=update

# Quote identifiers to avoid reserved-word clashes
spring.jpa.properties.hibernate.globally_quoted_identifiers=true

# Log SQL so you can see what runs
spring.jpa.show-sql=true

# App info
spring.application.name=covin counter
server.port=8080

# JWT settings (replace secret with a strong value before production)
app.jwt.secret=change-me-please-32chars-minimum-secret-key
app.jwt.expiration-ms=3600000
